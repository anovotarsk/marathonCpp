#ifndef VISITALL

#include <iostream>
#include <ostream>
#include <string>
#include <algorithm>
#include <deque>

std::string* strsplit(std::string str, char del, int *len);
void visitAll(int argc, char *argv[]);

#endif