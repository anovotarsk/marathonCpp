#ifndef STDALGOV2

#include <iostream>
#include <ostream>
#include <string>
#include <algorithm>
#include <forward_list>
#include <unistd.h>
#include <fstream>

std::string* strsplit(std::string str, char del, int *len);
void stdAlgoV2(char *file);

#endif
