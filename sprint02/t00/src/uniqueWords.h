#ifndef UNIQUEWORDS

#include <iostream>
#include <ostream>
#include <string>
#include <algorithm>
#include <set>
#include <unistd.h>
#include <fstream>
#include <regex>

std::string* strsplit(std::string str, char del, int *len);
void uniqueWords(char *file);

#endif
